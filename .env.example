# =============================================================================
# Stockify Environment Variables
# =============================================================================

# -----------------------------------------------------------------------------
# SERVER-SIDE ONLY (Vercel Environment Variables)
# These should be set in your Vercel project settings, NOT committed to git
# -----------------------------------------------------------------------------

# Google Gemini API Key (REQUIRED)
# Get your API key from: https://makersuite.google.com/app/apikey
# Set this in Vercel Dashboard > Project Settings > Environment Variables
GEMINI_API_KEY=your_gemini_api_key_here

# Upstash Redis (REQUIRED in production for rate limiting)
# Create a Redis database from Vercel Marketplace or Upstash console
# These should be set in Vercel Dashboard > Project Settings > Environment Variables
UPSTASH_REDIS_REST_URL=your_upstash_redis_rest_url
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_rest_token

# -----------------------------------------------------------------------------
# CLIENT-SIDE (Optional - embedded in build)
# These can be set locally for development
# -----------------------------------------------------------------------------

# Optional: Enable debug mode (shows additional logging in development)
REACT_APP_DEBUG=false

# Optional: Custom app title
REACT_APP_TITLE=Stockify

# Optional: Custom app description
REACT_APP_DESCRIPTION=AI-Powered Stock Photo Metadata Generator

# =============================================================================
# IMPORTANT SECURITY NOTE
# =============================================================================
# The GEMINI_API_KEY is now server-side only and should NEVER be prefixed with
# REACT_APP_ as that would expose it in the client-side JavaScript bundle.
#
# For local development:
#   1. Create a .env.local file (git-ignored)
#   2. Add: GEMINI_API_KEY=your_actual_key
#
# For Vercel deployment:
#   1. Go to Vercel Dashboard > Your Project > Settings > Environment Variables
#   2. Add GEMINI_API_KEY with your actual API key
#   3. DO NOT add it to any .env file that gets committed
# =============================================================================
